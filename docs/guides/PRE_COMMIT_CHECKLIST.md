# ✅ Pre-Commit Checklist

> Проверяй это перед каждым git commit!

## 🔍 Быстрая проверка

```bash
# Запусти эти команды перед commit:

# 1. Проверь что нет секретов в staged файлах
git diff --cached | grep -iE "password|secret|token|key.*=|api"

# 2. Проверь что .env и .tfvars НЕ добавлены
git status | grep -E "(\.env$|terraform\.tfvars)"

# 3. Убедись что tfstate файлы не tracked
git ls-files | grep tfstate

# Если любая из команд выше что-то нашла - исправь перед commit!
```

## 📋 Полный чеклист

### Файлы и директории
- [ ] `.env` файлы НЕ в git
- [ ] `terraform.tfvars` НЕ в git  
- [ ] `terraform.tfstate*` НЕ в git
- [ ] `*.json` (кроме package.json) НЕ в git
- [ ] `~/.yc/` НЕ в git
- [ ] SSH приватные ключи НЕ в git

### Документация
- [ ] Нет реальных паролей
- [ ] Нет реальных encryption keys
- [ ] Cloud ID / Folder ID заменены на примеры
- [ ] Service Account ID скрыты
- [ ] Пути относительные (не `/Users/...`)
- [ ] Email адреса только примеры или публичные

### Конфигурация
- [ ] `.gitignore` актуален
- [ ] `.env.example` обновлен (без реальных значений)
- [ ] `terraform.tfvars.example` обновлен

## ✅ Что МОЖНО коммитить

- ✅ Документация (*.md)
- ✅ Terraform файлы (*.tf)
- ✅ Docker Compose (docker-compose.yml)
- ✅ Nginx конфигурация
- ✅ Скрипты (*.sh)
- ✅ Шаблоны (.example файлы)
- ✅ .gitignore
- ✅ README и другие инструкции

## ❌ Что НЕЛЬЗЯ коммитить

- ❌ .env
- ❌ terraform.tfvars
- ❌ terraform.tfstate*
- ❌ *.json (service account keys)
- ❌ *.pem, *.key (SSH keys)
- ❌ *.log
- ❌ Бекапы (*.sql, *.tar.gz)

## 🔒 Перед первым push в новый репозиторий

```bash
# 1. Инициализируй git
git init

# 2. Проверь .gitignore
cat .gitignore

# 3. Добавь файлы
git add .

# 4. Проверь что добавлено (ничего чувствительного!)
git status

# 5. Проверь содержимое staged файлов
git diff --cached | less

# 6. Если всё ок - commit
git commit -m "Initial commit: n8n infrastructure"

# 7. Добавь remote
git remote add origin <URL>

# 8. Push
git push -u origin main
```

## 🚨 Если нашел секрет в staged

```bash
# Убери из staging
git reset HEAD <файл>

# Или сбрось весь staging
git reset HEAD

# Удали секрет из файла
# Затем снова add
```

## 📞 Дополнительная проверка

См. [SECURITY.md](SECURITY.md) для детальной информации о безопасности.

---

**Используй этот чеклист КАЖДЫЙ РАЗ перед commit!**
