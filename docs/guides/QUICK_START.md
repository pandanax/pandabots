# ⚡ Быстрый старт

> Ты здесь если хочешь быстро понять что делать дальше с проектом

## 🟢 Проект уже развернут и работает!

**n8n доступен:** https://n8n.mandala-app.online

## 🤖 Если ты AI агент - начни с этого:

1. **Прочитай [AI_AGENT_GUIDE.md](AI_AGENT_GUIDE.md)** - главная входная точка
2. **Прочитай [STATUS.md](STATUS.md)** - текущий статус
3. Теперь ты готов работать с проектом!

## 👨‍💻 Если ты человек:

### Доступ к n8n
Просто открой: https://n8n.mandala-app.online

### Работа с сервером

✅ **SSH через OS Login (рекомендуется):**

⚠️ **ВАЖНО:** Используй профиль `pandanax` для SSH!

```bash
# Проверить текущий профиль
yc config profile list  # ACTIVE = текущий

# Переключиться на нужный профиль (если нужно)
yc config profile activate pandanax

# Подключение к серверу
yc compute ssh --name n8n-server

# На сервере:
cd /opt/n8n
docker compose ps              # Статус
docker compose logs -f n8n     # Логи
docker compose restart n8n     # Перезапуск
```

**Выполнение команд удаленно:**
```bash
# ВАЖНО: сначала переключись на pandanax!
yc config profile activate pandanax

# Статус контейнеров
yc compute ssh --name n8n-server -- "cd /opt/n8n && docker compose ps"

# Логи n8n (последние 50 строк)
yc compute ssh --name n8n-server -- "cd /opt/n8n && docker compose logs --tail=50 n8n"

# Перезапуск n8n
yc compute ssh --name n8n-server -- "cd /opt/n8n && docker compose restart n8n"
```

**Если видишь ошибку "OS login info not found":**
```bash
# Это означает что активен неправильный профиль (sa-n8n-bot)
yc config profile activate pandanax  # переключись на личный профиль
yc compute ssh --name n8n-server     # теперь должно работать
```

### Альтернатива: Прямой SSH (для пользователя ubuntu)
```bash
ssh ubuntu@84.252.137.46  # Требуется SSH ключ в cloud-init
```

### Terraform изменения
```bash
cd terraform
terraform plan    # Проверить что изменится
terraform apply   # Применить изменения
```

## 📚 Документация

- **[AI_AGENT_GUIDE.md](AI_AGENT_GUIDE.md)** - для AI агентов
- **[STATUS.md](STATUS.md)** - текущий статус проекта
- **[README.md](README.md)** - полная документация
- **[deploy/README.md](deploy/README.md)** - управление Docker Compose

## 🆘 Проблемы?

1. Проверь [STATUS.md](STATUS.md) - там могут быть известные проблемы
2. Проверь логи: `docker compose logs`
3. Перезапусти: `docker compose restart`
4. Обратись к AI агенту через Cursor

## 🎯 Следующие шаги

- [ ] Открой n8n и создай admin аккаунт
- [ ] Создай первый workflow
- [ ] Настрой регулярные бекапы (см. STATUS.md)
- [ ] Настрой мониторинг (опционально)

---

**Статус:** 🟢 Всё работает  
**Последнее обновление:** 2026-02-01
