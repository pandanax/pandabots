# 📁 Локальные данные проекта

> Где хранятся все важные данные локально (не в git)

## 🎯 Основная идея

Все чувствительные данные (Cloud ID, Folder ID, пароли, ключи) хранятся **ЛОКАЛЬНО** в специальных файлах, которые **НЕ попадают в git**.

Это позволяет:
- ✅ AI агенту работать с реальными данными
- ✅ Тебе иметь быстрый доступ ко всей информации
- ✅ Безопасно публиковать проект в открытом репозитории

---

## 📂 Где хранятся данные

### 1. `.local/` - Быстрый доступ

```bash
.local/
├── project-config.env      # Переменные окружения
├── quick-reference.md      # Все данные в одном месте
└── README.md               # Описание
```

**Содержит:**
- Cloud ID и Folder ID
- Service Account ID
- IP адреса и домены
- Пути к ключам и конфигам
- Быстрые команды

**Использование:**
```bash
# Читать данные
cat .local/quick-reference.md

# Загрузить в сессию
source .local/project-config.env
echo $CLOUD_ID
echo $FOLDER_ID
```

### 2. `terraform/terraform.tfvars` - Terraform конфигурация

```bash
terraform/terraform.tfvars  # НЕ в git!
```

**Содержит:**
- Все Terraform переменные
- Cloud ID, Folder ID
- Пути к SSH ключам
- Пути к Service Account ключу
- IP адреса, настройки VM

**Использование:**
```bash
cat terraform/terraform.tfvars
cd terraform && terraform console
```

### 3. `deploy/.env` - Docker Compose секреты

```bash
deploy/.env  # НЕ в git!
```

**Содержит:**
- PostgreSQL пароль
- N8N encryption key (критично!)
- Let's Encrypt email
- Настройки n8n

**Использование:**
```bash
cat deploy/.env
# Или на сервере:
ssh ubuntu@84.252.137.46 "cat /opt/n8n/.env"
```

### 4. `~/.yc/` - Yandex Cloud CLI

```bash
~/.yc/n8n-sa-key.json       # Service Account ключ
~/.yc/config.yaml           # yc CLI конфигурация
```

**Содержит:**
- Service Account JSON ключ
- Профили yc CLI
- Токены доступа

---

## 🔒 Защита от git

Все эти файлы/директории добавлены в `.gitignore`:

```gitignore
# Локальная конфигурация
.local/

# Переменные окружения
.env
*.env
!.env.example

# Terraform
terraform/terraform.tfvars
terraform/*.tfstate*

# Yandex Cloud
.yc/
*.json
```

---

## 🤖 Для AI агента

Когда нужны реальные данные для работы:

1. **Читай `.local/quick-reference.md`** - там всё в одном месте
2. **Читай `terraform/terraform.tfvars`** - для Terraform команд
3. **Читай `deploy/.env`** - для Docker Compose (но НЕ показывай пароли!)

**Пример:**
```bash
# AI агент может выполнить:
cat .local/project-config.env
source .local/project-config.env

# И затем использовать:
yc compute instance list --folder-id $FOLDER_ID
```

---

## ⚠️ Важные правила

### ✅ МОЖНО:
- Читать эти файлы локально
- Использовать данные для команд
- Показывать Cloud ID, Folder ID, IP адреса
- Показывать пути к файлам

### ❌ НЕЛЬЗЯ:
- Коммитить эти файлы в git
- Показывать пароли и ключи шифрования
- Показывать содержимое Service Account JSON
- Показывать SSH приватные ключи

---

## 📋 Быстрая проверка

Проверь что все файлы на месте и не в git:

```bash
# Проверь что файлы существуют
ls -la .local/
ls -la terraform/terraform.tfvars
ls -la deploy/.env

# Проверь что они НЕ в git
git ls-files | grep -E "\.local|terraform\.tfvars|\.env$"
# Должно быть пусто!

# Проверь .gitignore
cat .gitignore | grep -E "\.local|terraform\.tfvars|\.env"
# Должны быть все файлы
```

---

## 🔄 Восстановление после клонирования

Если кто-то клонирует репозиторий, ему нужно создать эти файлы:

```bash
# 1. Создать .env из примера
cp deploy/.env.example deploy/.env
# Отредактировать deploy/.env с реальными данными

# 2. Создать terraform.tfvars из примера  
cp terraform/terraform.tfvars.example terraform/terraform.tfvars
# Отредактировать terraform/terraform.tfvars с реальными данными

# 3. .local/ создастся автоматически при первом запуске
# Или можно создать вручную на основе данных проекта
```

---

## 📖 См. также

- [SECURITY.md](SECURITY.md) - Правила безопасности
- [GIT_SETUP.md](GIT_SETUP.md) - Настройка git
- [AI_AGENT_GUIDE.md](AI_AGENT_GUIDE.md) - Для AI агентов

---

**Все данные безопасно хранятся локально!** ✅
