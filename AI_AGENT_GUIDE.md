# 🤖 AI Agent Guide - Входная точка для работы с проектом

> **ЭТО ГЛАВНЫЙ ФАЙЛ ДЛЯ AI АГЕНТОВ**  
> Прочитай его первым при начале работы с проектом

---

## 📋 Что это за проект?

**Название:** n8n на Yandex Cloud  
**Тип:** Production-ready развертывание системы автоматизации workflow  
**Статус:** 🟢 **РАБОТАЕТ И РАЗВЕРНУТ**

Это полностью автоматизированное развертывание n8n (open-source workflow automation) на Yandex Cloud с использованием Infrastructure as Code (Terraform) и Docker Compose.

---

## 🎯 Цель проекта

Создать и поддерживать production-ready сервер n8n для автоматизации бизнес-процессов с:
- ✅ Полной автоматизацией инфраструктуры (IaC)
- ✅ Безопасностью (SSL, HTTPS, Firewall)
- ✅ Надежностью (PostgreSQL, резервное копирование)
- ✅ Масштабируемостью (легко изменить через Terraform)

---

## 📊 ТЕКУЩИЙ СТАТУС ПРОЕКТА

⚠️ **ВСЕГДА ЧИТАЙ [STATUS.md](STATUS.md) ПЕРВЫМ!** ⚠️

**Последнее обновление:** 2026-02-01 14:30 UTC+3

### Что работает:
- ✅ n8n 2.4.8 развернут и доступен: https://n8n.mandala-app.online
- ✅ PostgreSQL 15 (база данных)
- ✅ Nginx с SSL/HTTPS
- ✅ Автоматическое обновление SSL сертификатов
- ✅ Вся инфраструктура управляется через Terraform

### Важная информация:
- **Domain:** n8n.mandala-app.online
- **SSH:** Данные для подключения в `.local/quick-reference.md` (открыт для всех, защищен Fail2ban)
- **Стоимость:** ~6000-7000 руб/мес
- **Git:** `git@github.com:pandanax/pandabots.git` (ветка: main)

---

## 🚨 КРИТИЧЕСКИ ВАЖНЫЕ ПРАВИЛА ДЛЯ AI АГЕНТА

### 1. РАБОТАЕМ С GIT!

⚠️ **ВАЖНО:** Проект использует **GIT**, не arc!

- Все изменения коммитим в git
- Используй стандартные git команды: `git add`, `git commit`, `git push`
- Remote: `git@github.com:pandanax/pandabots.git`
- Ветка: `main`

### 2. ВСЕГДА ОБНОВЛЯЙ ДОКУМЕНТАЦИЮ ПОСЛЕ КАЖДОГО УСПЕШНОГО ШАГА!

⚠️ **ЭТО ОБЯЗАТЕЛЬНОЕ ТРЕБОВАНИЕ!** ⚠️

После **КАЖДОГО** успешного выполнения задачи/шага ты ОБЯЗАН:

1. **Обновить [STATUS.md](STATUS.md)** - это самое важное!
   - Изменить дату и время
   - Добавить описание выполненного шага
   - Обновить статус компонентов если нужно
   - Добавить новые уроки в раздел "Что мы узнали"
   - Обновить TODO список (отметить выполненное, добавить новое)

2. **Создать git commit с изменениями**
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

3. **Создать новый документ в [docs/](docs/)** (если это крупное изменение)
   - История: `docs/NN-название-изменения.md`
   - Гайд: `docs/guides/название.md`
   - Справка: `docs/reference/название.md`
   - Обновить `docs/README.md` с новым документом

4. **Обновить [README.md](README.md)** (только если изменилась архитектура)

5. **Обновить этот файл** (только если изменились правила или структура)

**Правило:** Не считай задачу завершенной пока не обновил документацию и не закоммитил изменения!

### 3. Всегда следуй принципу Infrastructure as Code

- Все изменения инфраструктуры через Terraform
- Никаких ручных изменений в Yandex Cloud Console
- Все изменения коммитятся в git

### 4. Безопасность превыше всего

⚠️ **КРИТИЧЕСКИ ВАЖНО!** ⚠️

**НИКОГДА НЕ КОММИТЬ В GIT:**
- ❌ `.env` файлы
- ❌ Пароли, ключи шифрования, API ключи
- ❌ Токены ботов (Telegram, Discord, и т.д.)
- ❌ IP адреса серверов
- ❌ Cloud ID, Folder ID, OAuth токены
- ❌ SSH ключи, сертификаты
- ❌ Database credentials
- ❌ Любые секреты и чувствительные данные

**ОБЯЗАТЕЛЬНО ПРОВЕРЯЙ ПЕРЕД КОММИТОМ:**
1. Перечитай ВСЕ изменяемые файлы
2. Ищи паттерны: токены (цифры:буквы), пароли, IP адреса
3. Убедись что используешь плейсхолдеры: `<ВАШ_ТОКЕН>`, `<SERVER_IP>`, `<YOUR_PASSWORD>`
4. Если нашел секрет - НЕМЕДЛЕННО замени на плейсхолдер ПЕРЕД коммитом

**Где хранить чувствительные данные:**
- ✅ `.local/` директория (не в git)
- ✅ `.env` файлы (не в git, в .gitignore)
- ✅ n8n Credentials (в UI приложения)
- ✅ В памяти AI агента для текущей сессии (не записывать в файлы)

**Что делать если случайно закоммитил секрет:**
1. НЕМЕДЛЕННО удалить секрет из файла
2. Создать новый коммит с исправлением
3. Сообщить пользователю о необходимости revoke/rotate секрета
4. История git сохранит секрет - нужен rewrite истории или rotation секрета

---

## 🔑 Локальная конфигурация (важно!)

Все критические данные (Cloud ID, Folder ID, пароли) сохранены в локальных файлах:

```
.local/
├── project-config.env     # Cloud ID, Folder ID, IP, пути
├── quick-reference.md     # Быстрый доступ ко всем данным
└── README.md              # Описание директории
```

**Эти файлы НЕ в git!** Они доступны только локально для работы.

### Как использовать:

```bash
# Загрузить переменные в сессию
source .local/project-config.env

# Прочитать quick reference
cat .local/quick-reference.md

# Все данные также в:
cat terraform/terraform.tfvars  # Terraform конфигурация
cat deploy/.env                  # Docker Compose секреты
```

⚠️ **Для AI агента:** Читай эти файлы для получения актуальных данных, но не показывай пароли и ключи шифрования пользователю!

---

## 📂 Структура проекта

```
.
│
├── AI_AGENT_GUIDE.md          # ← ТЫ ЗДЕСЬ! Главная входная точка для AI
├── STATUS.md                   # ← ТЕКУЩИЙ СТАТУС - читай первым!
├── README.md                   # Главная страница проекта для людей
│
├── .local/                     # 🔒 Локальные данные (НЕ в git!)
│   ├── quick-reference.md     # ← ВСЕ ДАННЫЕ ЗДЕСЬ!
│   ├── project-config.env     # Cloud ID, Folder ID, пути
│   └── README.md              # Описание
│
├── docs/                       # 📚 Вся документация
│   ├── README.md              # Индекс документации
│   ├── 01-05-*.md             # История развертывания
│   ├── guides/                # Гайды и инструкции
│   │   ├── QUICK_START.md    # Быстрые команды
│   │   ├── GIT_SETUP.md      # Настройка git
│   │   └── PRE_COMMIT_CHECKLIST.md
│   └── reference/             # Справочная информация
│       ├── SECURITY.md       # Правила безопасности
│       ├── DATA_LOCATIONS.md # Где какие данные
│       └── LOCAL_DATA_GUIDE.md
│
├── terraform/                  # Infrastructure as Code
│   ├── *.tf                   # Terraform конфигурация
│   └── terraform.tfvars       # Переменные (НЕ в git!)
│
├── deploy/                     # Docker Compose
│   ├── docker-compose.yml     # Основная конфигурация
│   ├── .env                   # Секреты (НЕ в git!)
│   └── nginx/                 # Nginx конфигурация
│
└── scripts/                    # Автоматизация
    ├── deploy.sh              # Развертывание n8n
    ├── setup-ssl.sh           # Настройка SSL
    └── yc-wrapper.sh          # Yandex Cloud CLI wrapper
```

---

## 🔑 Ключевые файлы для понимания проекта

Читай в этом порядке:

1. **[STATUS.md](STATUS.md)** - текущий статус, что работает, планы
2. **Этот файл** - общая картина проекта, правила
3. **[docs/README.md](docs/README.md)** - индекс всей документации
4. **[docs/05-deployment-complete.md](docs/05-deployment-complete.md)** - детали деплоя и уроки
5. **[docs/reference/DATA_LOCATIONS.md](docs/reference/DATA_LOCATIONS.md)** - где какие данные

---

## 💡 Важные уроки и особенности проекта

### 1. VPN и динамические IP адреса
Пользователь работает через VPN, IP постоянно меняется.  
**Решение:** SSH открыт для всех (0.0.0.0/0), защита через Fail2ban.

### 2. Проблема "курицы и яйца" с SSL
Nginx не может запуститься без сертификата, certbot не может получить сертификат без Nginx.  
**Решение:** Двухэтапный процесс (HTTP-only → получить сертификат → включить HTTPS).

### 3. Docker Compose и .env файлы
`.env` ОБЯЗАТЕЛЬНО должен быть скопирован на сервер в ту же директорию что и `docker-compose.yml`.

### 4. DNS через Terraform
DNS зона и записи управляются через Terraform (`terraform/dns.tf`), не через веб-интерфейс.

Подробнее см. раздел "Что мы узнали" в [STATUS.md](STATUS.md).

---

## 🛠️ Типичные задачи и как их решать

### Задача: Пользователь просит что-то изменить в инфраструктуре

1. Найди нужный `.tf` файл в `terraform/`
2. Внеси изменения
3. `terraform plan` для проверки
4. `terraform apply` для применения
5. **ОБНОВИ STATUS.md с датой и описанием изменения**

### Задача: Проблема с n8n или контейнерами

1. Подключись по SSH: `ssh ubuntu@84.252.137.46`
2. Проверь логи: `cd /opt/n8n && docker compose logs`
3. Проверь статус: `docker compose ps`
4. Исправь проблему
5. **ОБНОВИ STATUS.md с описанием проблемы и решения**

### Задача: Добавить новый функционал

1. Спроси у пользователя уточняющие вопросы
2. Распиши план в чате
3. Реализуй изменения
4. Протестируй
5. **СОЗДАЙ НОВЫЙ ДОКУМЕНТ в docs/** если это крупное изменение
6. **ОБНОВИ STATUS.md**

### Задача: Пользователь начинает новый чат

1. Прочитай [STATUS.md](STATUS.md) первым!
2. Прочитай этот файл (AI_AGENT_GUIDE.md)
3. Теперь ты в контексте, готов работать

---

## 📡 Быстрые команды

### Проверка статуса
```bash
# SSH на сервер (IP в .local/quick-reference.md)
ssh ubuntu@<server-ip>

# Статус контейнеров
cd /opt/n8n && docker compose ps

# Логи n8n
docker compose logs -f n8n

# Проверка HTTPS
curl -I https://n8n.mandala-app.online/
```

### Terraform
```bash
cd terraform
terraform plan      # Проверить изменения
terraform apply     # Применить изменения
terraform show      # Показать текущее состояние
terraform output    # Показать outputs
```

### Docker Compose
```bash
cd /opt/n8n
docker compose ps                # Статус
docker compose logs -f [service] # Логи
docker compose restart [service] # Перезапуск
docker compose down              # Остановить всё
docker compose up -d             # Запустить всё
```

---

## 🔐 Важная информация о безопасности

### Что НЕ коммитить в git:
- `terraform/terraform.tfvars` (есть IP адреса)
- `deploy/.env` (есть пароли и ключи)
- Любые файлы с паролями, API ключами, сертификатами

### Что коммитить:
- Все `.tf` файлы
- `docker-compose.yml`
- `.env.example` (без реальных значений)
- Всю документацию
- Скрипты

---

## 📈 Мониторинг и здоровье проекта

### Что проверять регулярно:
- [ ] n8n доступен: https://n8n.mandala-app.online
- [ ] SSL сертификат валиден (expires: 2026-05-02)
- [ ] Все контейнеры в статусе "Up (healthy)"
- [ ] Логи не показывают критических ошибок

### Команды для проверки:
```bash
# Все контейнеры здоровы? (IP в .local/quick-reference.md)
ssh ubuntu@<server-ip> "cd /opt/n8n && docker compose ps"

# n8n отвечает?
curl -I https://n8n.mandala-app.online/

# Когда истекает SSL?
echo | openssl s_client -connect n8n.mandala-app.online:443 -servername n8n.mandala-app.online 2>/dev/null | openssl x509 -noout -dates
```

---

## 🎯 TODO для будущих улучшений

Смотри полный список в [STATUS.md](STATUS.md) в разделе TODO.

Основные пункты:
- Настроить автоматические бекапы PostgreSQL
- Добавить мониторинг (Prometheus/Grafana)
- Ограничить SSH когда пользователь не использует VPN
- Настроить rate limiting в Nginx

---

## 🤝 Как работать с пользователем

### Стиль общения:
- Всегда на русском языке
- Профессионально, но дружелюбно
- Задавай уточняющие вопросы при неоднозначности
- Предлагай лучшие практики, если видишь неоптимальное решение

### Когда обновлять документацию:
- После каждого значимого изменения
- Когда узнали что-то новое (проблему и решение)
- Когда достигли нового этапа (развертывание, настройка, и т.д.)

### Формат обновления STATUS.md:
```markdown
**Последнее обновление:** ГГГГ-ММ-ДД ЧЧ:ММ UTC+3

## ✅ Что изменилось (дата)
- Описание изменения 1
- Описание изменения 2

## 📚 Что мы узнали
### N. Название урока
- Проблема: ...
- Решение: ...
- Код/команды
```

---

## 📞 Полезные ссылки

- **n8n:** https://n8n.mandala-app.online
- **Yandex Cloud Console:** https://console.cloud.yandex.ru
- **n8n Docs:** https://docs.n8n.io/
- **Terraform Yandex Provider:** https://registry.terraform.io/providers/yandex-cloud/yandex/latest/docs

---

## ⚡ Быстрый чеклист для нового AI агента

Когда начинаешь работу с проектом:

- [ ] Прочитал [STATUS.md](STATUS.md)?
- [ ] Прочитал этот файл (AI_AGENT_GUIDE.md)?
- [ ] Понял текущий статус проекта?
- [ ] Понял что пользователь работает через VPN?
- [ ] Знаешь что после изменений нужно обновить STATUS.md?
- [ ] Знаешь структуру проекта?
- [ ] Готов работать!

---

**Версия документа:** 1.0  
**Создан:** 2026-02-01  
**Последнее обновление:** 2026-02-01 15:00 UTC+3

**Статус проекта:** 🟢 РАБОТАЕТ

---

## 🎓 Для следующего AI агента

Привет! Если ты читаешь это, значит начинаешь работу с проектом.

**Главное правило:** ВСЕГДА читай [STATUS.md](STATUS.md) первым, потом этот файл.

После этого ты будешь знать:
- Что это за проект (система автоматизации n8n)
- Текущий статус (работает, развернут, доступен)
- Что сделано и что осталось сделать
- Как работать с проектом
- Как обновлять документацию

Удачи! 🚀
