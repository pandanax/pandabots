# 🤖 Настройка DeepSeek AI в n8n

Пошаговая инструкция для подключения DeepSeek V3 к Telegram боту

---

## 🌟 Почему DeepSeek?

- ✅ **Работает из России** - нет блокировок
- ✅ **Очень дешево:** $0.27/$1.10 за 1M токенов (в 18 раз дешевле GPT-4!)
- ✅ **Качество близко к GPT-4** - отличные результаты
- ✅ **Быстрый** - отличная скорость ответов
- ✅ **Русский язык** - отлично работает
- ✅ **OpenAI-совместимый API** - легко интегрировать

---

## 📋 Что тебе понадобится

1. Telegram бот (уже есть)
2. n8n доступ: https://n8n.mandala-app.online
3. Email для регистрации на DeepSeek

---

## 🔑 Шаг 1: Получить DeepSeek API Key

### 1.1 Регистрация

1. **Открой:** https://platform.deepseek.com/
2. Нажми **"Sign Up"** или **"Get Started"**
3. Зарегистрируйся через:
   - Email
   - Google аккаунт
   - GitHub аккаунт

### 1.2 Получение API ключа

1. После входа перейди: **API Keys** (в меню слева)
2. Или напрямую: https://platform.deepseek.com/api_keys
3. Нажми **"Create API Key"**
4. Дай название ключу: например "n8n-telegram-bot"
5. **Скопируй API Key** - выглядит так: `sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

⚠️ **ВАЖНО:** Сохрани ключ сразу! Он показывается только один раз.

### 1.3 Пополнение баланса (опционально)

DeepSeek работает по **prepaid** модели:
1. Перейди: **Usage & Billing**
2. Нажми **"Add Credit"**
3. Минимум: $5 (хватит надолго при таких ценах!)
4. Способы оплаты: карта, PayPal, криптовалюта

**Для тестирования:** обычно дают небольшой welcome bonus.

---

## 💰 Цены DeepSeek V3 (очень дешево!)

| Модель | Input | Output | Качество |
|--------|-------|--------|----------|
| **deepseek-chat** | $0.27/1M токенов | $1.10/1M токенов | ⭐⭐⭐⭐⭐ |

**Для сравнения:**
- GPT-4o: $5/$15 за 1M токенов (в 18 раз дороже!)
- GPT-4o mini: $0.15/$0.60 за 1M токенов (в 2 раза дороже)

**Пример:**
- 1000 сообщений по 100 токенов (input) + 200 токенов (output)
- Стоимость: ~$0.25 (25 рублей!)

---

## 🔧 Шаг 2: Настроить Credentials в n8n

DeepSeek использует OpenAI-совместимый API, поэтому в n8n настраиваем как OpenAI credentials.

### 2.1 Создание Credential

1. Открой n8n: https://n8n.mandala-app.online
2. **Settings (⚙️)** → **Credentials**
3. **Add Credential** → найди **"OpenAI"**
4. Заполни:

```
Credential Name: DeepSeek API
API Key: <ТВО_API_KEY_ОТ_DEEPSEEK>
```

5. Нажми **Save**

⚠️ **Внимание:** Выбирай именно "OpenAI" credentials, НЕ "OpenAI Chat Model"!

---

## 📥 Шаг 3: Импортировать Workflow

1. Открой n8n: https://n8n.mandala-app.online
2. **Import from File** → выбери `telegram-deepseek-bot.json`
3. Workflow появится в списке

---

## 🔗 Шаг 4: Настроить Workflow

### 4.1 Подключить Credentials

В workflow нужно настроить **2 credentials**:

**1. Telegram Bot** (уже есть)
- Узлы: "Telegram Trigger" и "Send AI Reply"
- Выбери существующий credential

**2. DeepSeek API** (новый)
- Узел: "DeepSeek AI"
- Выбери credential "DeepSeek API"

### 4.2 Настроить Base URL (ВАЖНО!)

В узле **"DeepSeek AI"**:

1. Кликни на узел
2. В параметрах найди **"Options"** → **"Base URL"**
3. Убедись что указано: `https://api.deepseek.com`
4. Модель: `deepseek-chat` (основная модель)

---

## ⚙️ Шаг 5: Настройка параметров AI (опционально)

В узле **"DeepSeek AI"** можно настроить:

### Модель:
```
deepseek-chat  - Основная модель (рекомендуется)
```

### Temperature (температура):
```
0.0 - Детерминированные ответы (строго по фактам)
0.7 - Сбалансированная креативность (рекомендуется)
1.0 - Максимальная креативность
1.5 - Очень креативно (может "фантазировать")
```

### Max Tokens:
```
1000   - Короткие ответы (для Telegram - ОК)
2000   - Средние ответы
4000   - Длинные ответы
```

### System Prompt:

Системная инструкция для AI (можешь менять):
```
Ты дружелюбный AI ассистент в Telegram боте. 
Отвечай на русском языке, будь полезным и приветливым.
Давай конкретные и понятные ответы.
```

**Примеры кастомизации:**
```javascript
// Программист:
"Ты эксперт по Python. Помогай с кодом, давай рабочие примеры."

// Учитель:
"Ты репетитор. Объясняй сложное простыми словами, приводи примеры."

// Бизнес-помощник:
"Ты бизнес-консультант. Помогай с задачами предпринимателей."

// Веселый друг:
"Ты веселый собеседник. Шути, поддерживай хорошее настроение!"
```

---

## 🚀 Шаг 6: Активировать Workflow

1. Убедись что оба credentials настроены
2. **Сохрани workflow** (Ctrl+S)
3. Переключи **"Inactive" → "Active"**
4. n8n автоматически зарегистрирует webhook

---

## 🧪 Шаг 7: Тестирование

1. Открой своего бота в Telegram
2. Напиши любое сообщение:
   ```
   Привет! Расскажи о себе
   Помоги решить задачу по математике
   Напиши код на Python для сортировки списка
   Расскажи анекдот
   ```
3. Бот ответит используя DeepSeek V3! 🚀

---

## 🔍 Отладка

### Проблема 1: "Invalid API Key"
```
Решение:
1. Проверь что API Key скопирован полностью (начинается с sk-)
2. Проверь баланс: https://platform.deepseek.com/usage
3. Убедись что ключ не был удален
```

### Проблема 2: "Insufficient balance"
```
Причина: Недостаточно средств на балансе
Решение:
1. Пополни баланс: https://platform.deepseek.com/usage
2. Минимум $5 (хватит надолго!)
```

### Проблема 3: Бот не отвечает
```
Проверка:
1. Workflow активен (зеленый тумблер)
2. Оба credentials настроены правильно
3. Base URL = https://api.deepseek.com
4. В n8n → Executions смотри логи
```

### Проблема 4: Ошибка "Model not found"
```
Решение:
В узле DeepSeek AI проверь:
- Model: deepseek-chat (не deepseek-v3 или другое!)
```

### Проблема 5: Медленные ответы
```
Возможные причины:
1. Интернет соединение
2. DeepSeek может быть загружен (попробуй позже)
3. Слишком большой maxTokens (уменьши до 1000-2000)
```

---

## 📊 Структура Workflow

```
[Telegram Trigger]  - Получает сообщение от пользователя
       ↓
[Extract Data]      - Извлекает: текст, chatId, имя
       ↓
[DeepSeek AI]       - Обрабатывает через DeepSeek V3
       ↓
[Send AI Reply]     - Отправляет ответ в Telegram
```

### Описание узлов:

**1. Telegram Trigger**
- Тип: `telegramTrigger`
- Слушает входящие сообщения
- Webhook регистрируется автоматически

**2. Extract Data**
- Тип: `set`
- Извлекает из входящего сообщения:
  - `userMessage` - текст от пользователя
  - `chatId` - ID чата для ответа
  - `userName` - имя пользователя

**3. DeepSeek AI**
- Тип: `lmChatOpenAi` (OpenAI-compatible)
- Base URL: `https://api.deepseek.com`
- Model: `deepseek-chat`
- Отправляет запрос в DeepSeek
- Получает AI-сгенерированный ответ

**4. Send AI Reply**
- Тип: `telegram`
- Отправляет ответ пользователю
- Поддерживает Markdown форматирование

---

## 🎨 Продвинутая настройка

### Добавить историю диалога

Для запоминания контекста разговора нужно:
1. Сохранять историю в PostgreSQL (уже есть на сервере!)
2. Загружать последние N сообщений
3. Передавать в DeepSeek как контекст

(Могу создать отдельный workflow с памятью если нужно)

### Добавить команды

Перед узлом DeepSeek добавить узел **"Switch"**:
```
/start  → Приветственное сообщение
/help   → Справка по командам
/reset  → Очистить историю
/model  → Информация о модели
*       → Отправить в DeepSeek
```

### Ограничить длину сообщений

Telegram имеет лимит ~4096 символов на сообщение.
Добавь узел **"Code"** после DeepSeek:
```javascript
// Обрезать длинные ответы
let response = $json.response;
if (response.length > 4000) {
  response = response.substring(0, 3997) + "...";
}
return { response };
```

---

## 💡 Советы по оптимизации

### Экономия токенов:
1. Используй `maxTokens: 1000` для коротких ответов
2. Системная инструкция: "Отвечай кратко и по делу"
3. Не передавай длинную историю (максимум 5-10 сообщений)

### Улучшение качества:
1. Хороший system prompt (четкие инструкции)
2. Temperature 0.7 - золотая середина
3. Примеры в system prompt помогают AI понять формат

### Скорость:
1. DeepSeek уже быстрый
2. Можешь уменьшить maxTokens
3. Кеширование частых запросов (добавить узел cache)

---

## 📈 Мониторинг использования

### Проверка баланса и статистики:

1. **Дашборд:** https://platform.deepseek.com/usage
2. Видно:
   - Текущий баланс
   - Количество запросов
   - Токены использованные
   - Стоимость по дням

### Установка лимитов:

В настройках DeepSeek можно:
- Установить максимальную стоимость в день
- Получать уведомления при достижении порога
- Настроить rate limiting

---

## 🆚 DeepSeek vs другие модели

| Модель | Цена за 1M токенов | Доступность из РФ | Качество |
|--------|-------------------|-------------------|----------|
| **DeepSeek V3** | **$0.27/$1.10** | ✅ Да | ⭐⭐⭐⭐⭐ |
| GPT-4o | $5.00/$15.00 | ❌ Нет | ⭐⭐⭐⭐⭐ |
| GPT-4o mini | $0.15/$0.60 | ❌ Нет | ⭐⭐⭐⭐ |
| Claude 3.5 | $3.00/$15.00 | ❌ Нет | ⭐⭐⭐⭐⭐ |
| Gemini | БЕСПЛАТНО | ❌ Нет | ⭐⭐⭐⭐ |

**Вывод:** Для России DeepSeek - лучший выбор! 🏆

---

## 🚀 Дополнительные возможности DeepSeek

### 1. Streaming ответов
Можно включить streaming для постепенной отправки ответа:
```javascript
// В n8n можно настроить streaming responses
// Пользователь видит как AI "печатает"
```

### 2. Function calling
DeepSeek поддерживает вызов функций:
```javascript
// Можно добавить функции:
// - Получить погоду
// - Курс валют
// - Поиск в интернете
```

### 3. JSON mode
Для структурированных ответов:
```javascript
// DeepSeek может отвечать в JSON формате
// Удобно для интеграций
```

---

## 📚 Полезные ссылки

- **DeepSeek Platform:** https://platform.deepseek.com/
- **API Documentation:** https://platform.deepseek.com/api-docs/
- **Pricing:** https://platform.deepseek.com/pricing
- **Community:** https://discord.gg/deepseek (Discord)

---

## 🎓 Примеры использования

### Пример 1: Помощник программиста
```
System Prompt:
"Ты эксперт по программированию. Помогай с кодом на Python, JavaScript, Go. 
Давай рабочие примеры с комментариями. Объясняй понятно."

Пользователь: "Как сделать HTTP запрос в Python?"
DeepSeek: [Даст код с библиотекой requests и объяснением]
```

### Пример 2: Переводчик
```
System Prompt:
"Ты переводчик с английского на русский и обратно. 
Переводи точно, сохраняй смысл и стиль."

Пользователь: "Translate: The quick brown fox"
DeepSeek: "Быстрая коричневая лиса"
```

### Пример 3: Бизнес-ассистент
```
System Prompt:
"Ты помощник предпринимателя. Помогай с задачами бизнеса:
планирование, анализ, маркетинг, финансы."

Пользователь: "Как увеличить продажи в интернет-магазине?"
DeepSeek: [Даст конкретные стратегии]
```

---

⚠️ **Безопасность:** НЕ публикуй API ключ в открытом доступе!  
Храни его только в n8n Credentials.

---

**Готово!** Теперь у тебя мощный AI бот, работающий из России! 🇷🇺🤖
