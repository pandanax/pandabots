# 🎨 Настройка Mandala Bot Advanced

Продвинутый AI бот-специалист по персональным мандалам с историей диалога и базой знаний (RAG)

---

## 🌟 Возможности бота

- ✅ **Специализация:** Эксперт по созданию персональных мандал
- ✅ **История диалога:** Помнит контекст разговора для каждого пользователя
- ✅ **RAG (База знаний):** Использует знания о мандалах из `rags/mandala-bot-advanced/`
- ✅ **Персонализация:** Рассчитывает мандалы по дате рождения и имени
- ✅ **Инструкции:** Дает пошаговые инструкции как рисовать мандалу
- ✅ **Multi-user:** Отдельная история для каждого пользователя

---

## 📋 Требования

1. **PostgreSQL** - для хранения истории (уже есть на сервере!)
2. **DeepSeek API ключ** (уже есть)
3. **Telegram Bot** (уже настроен: MandalaBot)
4. **База знаний** - файлы в `rags/mandala-bot-advanced/` (уже есть)

---

## 🗄️ Шаг 1: Создать таблицу в PostgreSQL

⚠️ **Используем Yandex Managed PostgreSQL!**

### 1.1 Создать таблицу через YC CLI

```bash
# Локально (с твоего компьютера)
cd terraform

# Получить ID кластера
CLUSTER_ID=$(terraform output -raw postgres_cluster_id)

# Создать таблицу
yc managed-postgresql cluster execute \
  --id $CLUSTER_ID \
  --database n8n \
  --user n8n \
  << 'EOF'
CREATE TABLE IF NOT EXISTS chat_history (
    id SERIAL PRIMARY KEY,
    user_id BIGINT NOT NULL,
    role VARCHAR(20) NOT NULL,
    content TEXT NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_user_created ON chat_history(user_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_user_id ON chat_history(user_id);
EOF
```

### 1.2 Проверить что таблица создана

```bash
# Список таблиц
yc managed-postgresql cluster execute \
  --id $CLUSTER_ID \
  --database n8n \
  --user n8n \
  -c "\dt"

# Структура таблицы
yc managed-postgresql cluster execute \
  --id $CLUSTER_ID \
  --database n8n \
  --user n8n \
  -c "\d chat_history"
```

### 1.3 (Опционально) Очистить старые сообщения

```bash
# Удалить сообщения старше 30 дней
yc managed-postgresql cluster execute \
  --id $CLUSTER_ID \
  --database n8n \
  --user n8n \
  -c "DELETE FROM chat_history WHERE created_at < NOW() - INTERVAL '30 days';"

# Очистить историю конкретного пользователя
yc managed-postgresql cluster execute \
  --id $CLUSTER_ID \
  --database n8n \
  --user n8n \
  -c "DELETE FROM chat_history WHERE user_id = 123456789;"

# Полная очистка таблицы
yc managed-postgresql cluster execute \
  --id $CLUSTER_ID \
  --database n8n \
  --user n8n \
  -c "TRUNCATE TABLE chat_history;"
```

---

## 🔑 Шаг 2: Настроить Credentials в n8n

### 2.1 PostgreSQL Credentials (Yandex Managed PostgreSQL)

1. n8n → **Settings** → **Credentials** → **Add Credential**
2. Выбери **"PostgreSQL"**
3. Заполни:

```
Credential Name: PostgreSQL
Host: <POSTGRES_HOST из terraform output>
Database: n8n
User: n8n
Password: <POSTGRES_PASSWORD из terraform/terraform.tfvars>
Port: 6432
SSL: Enable
```

**Как получить Host:**
```bash
cd terraform
terraform output postgres_host
# Вернет: c-xxxxxxxxx.rw.mdb.yandexcloud.net
```

**Где взять Password:**
```bash
cat terraform/terraform.tfvars | grep postgres_password
```

4. **Test Connection** → должно быть OK
5. **Save**

### 2.2 DeepSeek API Credentials

1. n8n → **Settings** → **Credentials** → **Add Credential**
2. Выбери **"OpenAI"**
3. Заполни:

```
Credential Name: DeepSeek API
API Key: <ТВОЙ_DEEPSEEK_API_KEY>
```

4. **Save**

⚠️ **Важно:** Telegram Bot credentials (MandalaBot) уже настроены!

---

## 📂 Шаг 3: Подготовить базу знаний (RAG)

База знаний уже есть в `rags/mandala-bot-advanced/` и загружена на сервер!

### Структура базы знаний:

```
rags/mandala-bot-advanced/
  ├── m1              - Основные знания о мандалах
  ├── m2              - Алгоритм рисования
  ├── knowledge.txt   - Объединённый файл (используется в workflow)
  └── README.md       - Документация
```

### В узле "Read Knowledge Base":

Путь уже настроен на сервере:

```
File Path: /home/node/.n8n-files/rags/mandala-bot-advanced/knowledge.txt
```

⚠️ **Важно:** Файлы уже загружены на сервер с правильными правами!

### Как обновить базу знаний:

1. Отредактируй локально `rags/mandala-bot-advanced/m1` или `m2`
2. Пересобери объединенный файл:
   ```bash
   cat rags/mandala-bot-advanced/m1 rags/mandala-bot-advanced/m2 > rags/mandala-bot-advanced/knowledge.txt
   ```
3. Загрузи на сервер:
   ```bash
   scp rags/mandala-bot-advanced/knowledge.txt ubuntu@84.252.137.46:/tmp/
   ssh ubuntu@84.252.137.46 "sudo mv /tmp/knowledge.txt /home/node/.n8n-files/rags/mandala-bot-advanced/ && sudo chown 1000:1000 /home/node/.n8n-files/rags/mandala-bot-advanced/knowledge.txt"
   ```

Подробнее см. `rags/mandala-bot-advanced/README.md`

---

## 📥 Шаг 4: Импортировать Workflow

1. n8n → **Import from File**
2. Выбери: `mandala-bot-advanced.json`
3. Workflow появится в списке

---

## 🔗 Шаг 5: Настроить все Credentials в Workflow

Проверь что все узлы используют правильные credentials:

### ✅ Telegram Trigger
- Credential: **MandalaBot** ✅ (уже настроен)

### ✅ Load Chat History
- Credential: **PostgreSQL** (создали в шаге 2.1)

### ✅ DeepSeek AI
- Credential: **DeepSeek API** (создали в шаге 2.2)

### ✅ Save to History
- Credential: **PostgreSQL** (тот же что в Load)

### ✅ Send AI Reply
- Credential: **MandalaBot** ✅ (уже настроен)

---

## ⚙️ Шаг 6: Настройка параметров (опционально)

### В узле "DeepSeek AI":

```javascript
temperature: 0.8       // Креативность (0.0-1.5)
maxTokens: 1000        // Макс длина ответа (ограничено для Telegram, лимит 4096 символов)
baseURL: https://api.deepseek.com
```

### В узле "Load Chat History":

```sql
LIMIT 10  -- Загружать последние 10 сообщений
```

Можешь изменить на 5, 20, 50 в зависимости от нужного контекста.

### В узле "Build AI Context":

System Prompt можешь донастроить:
- Добавить примеры мандал
- Изменить стиль общения
- Добавить специфичные инструкции

---

## 🚀 Шаг 7: Активировать Workflow

1. Убедись что все credentials настроены ✅
2. Убедись что PostgreSQL таблица создана ✅
3. Убедись что база знаний доступна ✅
4. **Save workflow** (Ctrl+S)
5. Переключи **"Inactive" → "Active"**

n8n автоматически зарегистрирует webhook!

---

## 🧪 Шаг 8: Тестирование

### Тест 1: Простое приветствие

```
Пользователь: Привет!
Бот: [Приветствует и рассказывает о себе как специалисте по мандалам]
```

### Тест 2: Запрос мандалы

```
Пользователь: Хочу создать персональную мандалу
Бот: [Спрашивает какой тип мандалы, запрашивает дату рождения]
```

### Тест 3: Предоставление данных

```
Пользователь: Моя дата рождения 27.11.1991
Бот: [Начинает расчет нумерологической мандалы, выдает результат и инструкции]
```

### Тест 4: Проверка истории

```
Пользователь: А какую дату я тебе дал?
Бот: [Вспоминает дату 27.11.1991 из истории диалога]
```

---

## 📊 Структура Workflow

```
[Telegram Trigger] - Получает сообщение от пользователя
       ↓
[Extract User Data] - Извлекает userId, chatId, message, timestamp
       ↓
[Load Chat History] ← PostgreSQL - Загружает последние 10 сообщений пользователя
       ↓
[Format History] - Форматирует историю для AI
       ↓
[Read Knowledge Base] → [Load RAG Knowledge] - Загружает базу знаний о мандалах
       ↓
[Build AI Context] - Объединяет: system prompt + история + RAG + текущий вопрос
       ↓
[DeepSeek AI] - Генерирует умный ответ специалиста по мандалам
       ↓
[Save to History] → PostgreSQL - Сохраняет вопрос и ответ в БД
       ↓
[Send AI Reply] - Отправляет ответ в Telegram с Markdown форматированием
```

### Ключевые узлы:

**1. Load Chat History** (PostgreSQL)
- Загружает последние N сообщений конкретного пользователя
- ORDER BY created_at DESC (сначала новые)
- Каждый пользователь имеет свою историю (по user_id)

**2. Build AI Context** (Code Node)
- Формирует массив messages для DeepSeek
- Добавляет system prompt с ролью специалиста по мандалам
- Вставляет базу знаний из RAG
- Добавляет историю диалога
- Добавляет текущее сообщение

**3. DeepSeek AI**
- Обрабатывает весь контекст
- Генерирует ответ специалиста
- temperature: 0.8 (баланс между точностью и креативностью)
- max_tokens: 1000 (ограничено для Telegram, лимит 4096 символов на сообщение)

**4. Save to History**
- Сохраняет ОБА сообщения: от user и от assistant
- Использует один INSERT с двумя строками

---

## 🔍 Отладка

### Проблема 1: "Table chat_history does not exist"

```
Решение:
1. Подключись к PostgreSQL
2. Создай таблицу (см. Шаг 1.2)
3. Проверь: SELECT * FROM chat_history;
```

### Проблема 2: PostgreSQL Credential ошибка

```
Решение:
1. Проверь Host: должен быть "postgres" (имя контейнера)
2. Проверь Password из deploy/.env
3. Test Connection в n8n
```

### Проблема 3: База знаний не загружается

```
Решение:
1. Проверь путь к файлу на сервере: /home/node/.n8n-files/rags/mandala-bot-advanced/knowledge.txt
2. Проверь что файл существует: ssh ubuntu@84.252.137.46 "sudo cat /home/node/.n8n-files/rags/mandala-bot-advanced/knowledge.txt | head -10"
3. Проверь права доступа: должен быть доступ для чтения
```

### Проблема 4: Бот не помнит историю

```
Проверка:
1. В PostgreSQL: SELECT * FROM chat_history WHERE user_id=<твой_user_id>;
2. Должны быть записи с role='user' и role='assistant'
3. В n8n Executions смотри узел "Load Chat History" - есть ли данные
```

### Проблема 5: Бот отвечает не как специалист

```
Решение:
1. Проверь узел "Build AI Context"
2. Убедись что system prompt содержит инструкцию про мандалы
3. Проверь что база знаний загрузилась (узел "Load RAG Knowledge")
```

---

## 💡 Советы по использованию

### Типичный диалог с ботом:

```
Пользователь: Привет! Что ты умеешь?
Бот: Здравствуйте! Я специалист по созданию персональных мандал...

Пользователь: Хочу нумерологическую мандалу
Бот: Отлично! Для нумерологической мандалы мне понадобится ваша дата рождения...

Пользователь: 15.03.1985
Бот: Спасибо! Начинаю расчёт вашей персональной мандалы...
[Выдает расчеты, цвета, значения, инструкции]

Пользователь: А что означает мой доминирующий цвет?
Бот: [Вспоминает из истории и объясняет]
```

### Оптимизация истории:

```sql
-- Оставлять только последние 20 сообщений на пользователя
-- Добавь в cron job (выполнять раз в день):
DELETE FROM chat_history 
WHERE id NOT IN (
    SELECT id FROM (
        SELECT id FROM chat_history 
        WHERE user_id = chat_history.user_id 
        ORDER BY created_at DESC 
        LIMIT 20
    ) subquery
);
```

---

## 🎨 Расширение возможностей

### Добавить команды:

Перед узлом "Load Chat History" добавь узел **"Switch"**:

```
/start  → Приветствие и описание бота
/help   → Справка по командам
/reset  → Очистить историю (DELETE WHERE user_id=X)
/types  → Показать типы мандал
*       → Обычный диалог с AI
```

### Добавить больше знаний:

1. Создай файлы: `rags/m2`, `rags/m3` с дополнительной информацией
2. В узле "Read Knowledge Base" загружай несколько файлов
3. Объединяй в "Build AI Context"

### Генерация изображений:

Для визуализации мандал можно:
1. Интегрировать DALL-E / Midjourney API
2. Или использовать библиотеку Python для генерации
3. Отправлять изображение через Telegram (узел "Send Photo")

### Экспорт мандалы:

Добавить возможность экспорта:
1. Генерировать PDF с расчетами
2. Создавать изображение мандалы
3. Отправлять файл пользователю

---

## 📈 Мониторинг

### Проверка истории:

```sql
-- Сколько всего сообщений
SELECT COUNT(*) FROM chat_history;

-- Сколько уникальных пользователей
SELECT COUNT(DISTINCT user_id) FROM chat_history;

-- Топ активных пользователей
SELECT user_id, COUNT(*) as messages 
FROM chat_history 
GROUP BY user_id 
ORDER BY messages DESC 
LIMIT 10;

-- История конкретного пользователя
SELECT role, content, created_at 
FROM chat_history 
WHERE user_id = <USER_ID> 
ORDER BY created_at DESC 
LIMIT 20;
```

---

## 🔒 Безопасность

### Данные пользователей:

- ✅ История хранится в PostgreSQL (изолирован в Docker)
- ✅ Каждый пользователь видит только свою историю
- ✅ API ключи в credentials (не в коде)

### Очистка данных:

По GDPR или по запросу пользователя:

```sql
-- Удалить всю историю пользователя
DELETE FROM chat_history WHERE user_id = <USER_ID>;
```

### Резервное копирование:

```bash
# Бекап таблицы chat_history
docker exec n8n-postgres pg_dump -U n8n -t chat_history n8n > chat_history_backup.sql

# Восстановление
cat chat_history_backup.sql | docker exec -i n8n-postgres psql -U n8n -d n8n
```

---

## 📚 База знаний (RAG)

Текущая база знаний в `rags/mandala-bot-advanced/` содержит:
- **m1**: Что такое мандалы, типы, персональные мандалы по дате рождения, нумерологический и астрологический методы
- **m2**: Алгоритм рисования мандалы, геометрические правила, законы классической мандалы
- **knowledge.txt**: Объединённый файл для использования в workflow

### Обновление базы знаний:

1. Отредактируй файл `rags/mandala-bot-advanced/m1` или `m2`
2. Пересобери и загрузи на сервер (см. раздел "Шаг 3" выше)
3. Деактивируй workflow и активируй заново

---

**Готово!** Теперь у тебя мощный AI-бот специалист по мандалам! 🎨🤖

⚠️ **Важно:** Не забудь создать таблицу в PostgreSQL перед запуском!
